const Headings = ({ title }) => {
  return (
    <div className="flex justify-center items-baseline">
      <h3 className="text-[#0B0B0B] font-bold text-4xl">{title}</h3>
    </div>
  );
};

export default Headings;
